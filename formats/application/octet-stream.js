define(function() {

  'use strict';
  
  // very important that this format handler be present & valid
  // as any file format that doesn't load its own handler will
  // try to load this instead
  
  return {
  };

});
